{
  "name": "designedforyou",
  "version": "0.1.0",
  "description": "Designed For You - Professional Web Development Services",
  "scripts": {
    "dev": "npx http-server . -p 3001",
    "build": "mkdir -p dist && cp -r *.html assets public src components dist/ && [ -d styles ] && cp -r styles dist/ || true",
    "start": "npx http-server dist -p 3001"
  },
  "dependencies": {
    "http-server": "^14.1.1"
  }
}
